/**
 * App-owned port interfaces (AD-2). Canonical definitions live in
 * `src/core/ports.ts`; these files give them the AD-2 layout without the pure
 * engine importing outward.
 */
export type * from "@/core/ports";
