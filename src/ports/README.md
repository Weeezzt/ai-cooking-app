# src/ports

App-owned interfaces the core depends on (inward-facing). No implementations here.

Planned (AD-2 / AD-9): `StoreDiscovery.ts`, `ProductSearch.ts`, `PriceSource.ts`,
`NutritionSource.ts`, `RecipeGenerator.ts`.
