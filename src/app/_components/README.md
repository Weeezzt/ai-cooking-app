# src/app/_components

Shared React components and per-mode UI. Underscore-prefixed so Next.js treats it
as a private folder (never routable). Design primitives land here in Issue #2;
PLAN/SHOP/COOK components in Issues #8–#10. Components render `PlanResult`; they
never contain basket/nutrition/constraint logic (engineering-rules).
